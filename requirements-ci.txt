# CI-only, hash-pinned: a re-uploaded or replaced wheel cannot run in a job that holds a token.
# Kept in lockstep with the Dockerfile's pillow==12.3.0. Refresh the hash with:
#   python3 -m pip download pillow==12.3.0 --no-deps --only-binary=:all: \
#     --platform manylinux_2_28_x86_64 --python-version 3.12 --implementation cp -d /tmp/pw
#   python3 -m pip hash /tmp/pw/*.whl
pillow==12.3.0 --hash=sha256:78cb2c6865a35ab8ff8b75fd122f6033b92a62c82801110e48ddd6c936a45d91
